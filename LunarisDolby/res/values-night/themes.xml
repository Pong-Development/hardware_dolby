<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Dolby" parent="android:Theme.DeviceDefault.NoActionBar">
        <!-- Edge to edge: Compose paints the background, bars stay transparent and
             the platform must not draw its own contrast scrim over them. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <!-- Dark mode wants light-on-dark bar icons, the inverse of the day theme. -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
